# Window.imeMode

機能/意味
:   デフォルトのIMEモード

タイプ
:   [Windowクラス](f_Window)のプロパティ (読み出し専用)

説明
:   デフォルトのIMEモードを表します。
    　ここで指定したモードは、どのレイヤにもフォーカスが無い状態に設定されるモードです。
    　未指定の場合は imDisableで、これはどのレイヤにもフォーカスが無い状態では IME は無効状態であるということになります。
    　指定可能な値については [Layer.imeMode](f_Layer_imeMode) を参照してください。