# Clipboard.asTJS

機能/意味
:   クリップボードにTJS式を登録する、もしくは取り出す

タイプ
:   [Clipboardクラス](class_Clipboard)のプロパティ

説明
:   取得に失敗したらvoidを返す。
    TJS式は、メンバー「type」(文字列)および「body」(型を問わない)を持った辞書で無ければならない。
    それ以外の形式のデータを登録しようとすると登録が失敗する。