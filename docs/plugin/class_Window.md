# Window

# メンバ

メソッド
:   [bringTo](func_Window_bringTo) (ウィンドウの Zオーダーを変更する )
    [cancelCheckSignature](func_Window_cancelCheckSignature) (署名チェックのキャンセル )
    [cancelSaveLayerImage](func_Window_cancelSaveLayerImage) (画像保存キャンセル )
    [checkSignature](func_Window_checkSignature) (署名チェックの開始 )
    [commandExecute](func_Window_commandExecute) (コンソール出力取得つきコマンドラインプログラムの実行 )
    [commandSendSignal](func_Window_commandSendSignal) (Ctrl+Cイベントの送信(experimental:すべての環境で動作するとは限りません) )
    [doProgress](func_Window_doProgress) (プログレス処理の経過状態を通知する。 )
    [endProgress](func_Window_endProgress) (プログレス処理を終了する。 )
    [focusMenuByKey](func_Window_focusMenuByKey) (SC\_KEYMENUを送る )
    [forceResizeRect](func_Window_forceResizeRect) (（※これはプラグインで拡張される関数ではありません！） onResizingでサイズを固定させるサンプル )
    [getClientRect](func_Window_getClientRect) (クライアント・ウィンドウ領域，非最大化通常表示の領域の取得 )
    [getNormalRect](func_Window_getNormalRect) (非最大化通常表示領域(@param nofix true:ワークエリア補正を行わない) )
    [getNotificationName](func_Window_getNotificationName) (イベント番号からWM\_XXXを取得（WM\_を除いて返る） )
    [getNotificationNum](func_Window_getNotificationNum) (WM\_XXXイベント番号を取得（WM\_は除いて渡す） )
    [getWindowRect](func_Window_getWindowRect) (ウィンドウ領域 )
    [maximize](func_Window_maximize) (ウィンドウの最大化 )
    [minimize](func_Window_minimize) (ウィンドウの最小化（mainWindowではタスクバー収納，それ以外は画面左下に最小化） )
    [ncHitTest](func_Window_ncHitTest) (非クライアントエリア調査 )
    [onAccelKeyMenu](func_Window_onAccelKeyMenu) (WM\_MENUCHAR通知コールバック )
    [onActivateChanged](func_Window_onActivateChanged) (■拡張イベント：アクティブ状態変更通知 )
    [onCheckSignatureDone](func_Window_onCheckSignatureDone) (イベント:署名チェック終了 )
    [onCheckSignatureProgress](func_Window_onCheckSignatureProgress) (イベント:署名チェック進捗 )
    [onCommandLineOutput](func_Window_onCommandLineOutput) (イベント:コンソール出力 )
    [onDisplayChanged](func_Window_onDisplayChanged) (■拡張イベント：ディスプレイ構成の変更通知 )
    [onDrawClipboard](func_Window_onDrawClipboard) (クリップボードが更新された時に呼び出されるフック関数 )
    [onEnterMenuLoop](func_Window_onEnterMenuLoop) (■拡張イベント：メニュー処理の開始・終了通知 )
    [onExitMenuLoop](func_Window_onExitMenuLoop) (< メニュー処理開始 )
    [onHide](func_Window_onHide) (■拡張イベント：ウィンドウ非表示 )
    [onMaximize](func_Window_onMaximize) (■拡張イベント：ウィンドウ最大化 )
    [onMaximizeQuery](func_Window_onMaximizeQuery) (■拡張イベント：最大化を許可するかどうか確認 )
    [onMessageReceived](func_Window_onMessageReceived) (イベント：メッセージ受信。 )
    [onMinimize](func_Window_onMinimize) (■拡張イベント：ウィンドウ最小化 )
    [onMonitorPower](func_Window_onMonitorPower) ( )
    [onMove](func_Window_onMove) (■拡張イベント：ウィンドウ移動（完了） )
    [onMoveSizeBegin](func_Window_onMoveSizeBegin) (■拡張イベント：リサイズ・移動開始通知/終了 )
    [onMoveSizeEnd](func_Window_onMoveSizeEnd) (終了通知 )
    [onMoving](func_Window_onMoving) (■拡張イベント：ウィンドウ移動中 )
    [onNcMouseDown](func_Window_onNcMouseDown) ( )
    [onNcMouseLeave](func_Window_onNcMouseLeave) ( )
    [onNcMouseMove](func_Window_onNcMouseMove) (■拡張イベント：非クライアントエリアマウスイベント )
    [onNcMouseUp](func_Window_onNcMouseUp) (< registerExEvent()後の追加は無効（詳細はregisterExEventの説明を参照） )
    [onNonCapMouseEvent](func_Window_onNonCapMouseEvent) (WM\_SETCURSORコールバック：ht=LOWORD(LParam), ev=HIWORD(LParam) )
    [onResizing](func_Window_onResizing) (■拡張イベント：サイズ変更中 )
    [onSaveLayerImageDone](func_Window_onSaveLayerImageDone) (保存処理実行完了イベント )
    [onSaveLayerImageProgress](func_Window_onSaveLayerImageProgress) (保存処理実行中イベント )
    [onScreenSave](func_Window_onScreenSave) (■拡張イベント：スクリーンセーバー開始通知，モニタパワー変更通知 )
    [onShellExecuted](func_Window_onShellExecuted) (イベント:シェル実行終了 )
    [onShow](func_Window_onShow) (■拡張イベント：ウィンドウ表示 )
    [onStartKeyMenu](func_Window_onStartKeyMenu) (SC\_KEYMENU通知コールバック )
    [onWindowsMessageHook](func_Window_onWindowsMessageHook) (setMessageHookで指定したイベント発生時のコールバック )
    [registerExEvent](func_Window_registerExEvent) (拡張イベントを有効にする )
    [registerUserMessageReceiver](func_Window_registerUserMessageReceiver) (ユーザ定義のメッセージハンドラを登録します。 )
    [resetWindowIcon](func_Window_resetWindowIcon) (ウィンドウのタイトルバーアイコンを再設定する )
    [sendMessage](func_Window_sendMessage) (メッセージの送信を行います。 )
    [sendToBack](func_Window_sendToBack) (/ alias method )
    [sendUserMessage](func_Window_sendUserMessage) (低レベルメッセージ送信を実行します。 )
    [setMessageHook](func_Window_setMessageHook) (メッセージフックを設定(on:有効, ev:イベント番号またはイベント名文字：省略時全部） )
    [setOverlayBitmap](func_Window_setOverlayBitmap) (オーバーレイビットマップを表示・消去する )
    [setProgressMessage](func_Window_setProgressMessage) (プログレス処理のテキストを差し替える )
    [setWindowIcon](func_Window_setWindowIcon) (ウィンドウのタイトルバーアイコンを変更する )
    [shellExecute](func_Window_shellExecute) (バックグラウンドでの外部プログラムの実行 )
    [showRestore](func_Window_showRestore) (ウィンドウ最大化・最小化の復帰 )
    [startProgress](func_Window_startProgress) (プログレス処理を開始する。 吉里吉里が実行ブロック中でも正常に表示継続します。 )
    [startSaveLayerImage](func_Window_startSaveLayerImage) (TLG5/PNG 形式での画像の保存の開始 )
    [stopCheckSignature](func_Window_stopCheckSignature) (署名チェックの中止 )
    [stopSaveLayerImage](func_Window_stopSaveLayerImage) (画像保存中止（中止した場合は終了イベントが来ません） )
    [terminateProcess](func_Window_terminateProcess) (実行中の外部プログラムの強制終了 )

プロパティ
:   [clipboardWatchEnabled](prop_Window_clipboardWatchEnabled) (クリップボード更新の監視をするかどうか設定する(デフォルトはオフ) )
    [disableResize](prop_Window_disableResize) (リサイズの抑制 )
    [enableNCMouseEvent](prop_Window_enableNCMouseEvent) (WM\_SETCURSORのコールバックonNonCapMouseEventを有効にする（デフォルト：false） )
    [exSystemMenu](prop_Window_exSystemMenu) (システムメニューに追加 )
    [maximized](prop_Window_maximized) (ウィンドウが最大化かどうか取得 )
    [messageEnable](prop_Window_messageEnable) (メッセージ受信機能が有効かどうかを指定します。 )
    [minimized](prop_Window_minimized) (ウィンドウが最小化かどうか取得 )
    [storeHWND](prop_Window_storeHWND) (HWND 情報を保存するかどうかを指定します。 )

定数
:   nchtBorder (HTBORDER )
    nchtBottom (HTBOTTOM )
    nchtBottomLeft (HTBOTTOMLEFT )
    nchtBottomRight (HTBOTTOMRIGHT )
    nchtCaption (HTCAPTION )
    nchtClient (HTCLIENT )
    nchtError (HTERROR )
    nchtGrowBox (HTGROWBOX )
    nchtHScroll (HTHSCROLL )
    nchtLeft (HTLEFT )
    nchtMaxButton (HTMAXBUTTON )
    nchtMenu (HTMENU )
    nchtMinButton (HTMINBUTTON )
    nchtNoWhere (HTNOWHERE )
    nchtReduce (HTREDUCE )
    nchtRight (HTRIGHT )
    nchtSize (HTSIZE )
    nchtSysMenu (HTSYSMENU )
    nchtTop (HTTOP )
    nchtTopLeft (HTTOPLEFT )
    nchtTopRight (HTTOPRIGHT )
    nchtTransparent (HTTRANSPARENT )
    nchtVScroll (HTVSCROLL )
    nchtZoom (HTZOOM )
    PBS\_SMOOTH (プログレスバースムーズ指定 )
    PBS\_VERTICAL (プログレスバー垂直配置指定 )