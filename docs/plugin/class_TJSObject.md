# TJSObject

Javascript 側で createTJSClass して生成されたインスタンス中では
以下の特殊なメソッドが利用可能です。イベントの伝播などで活用可能です。

# メンバ

メソッド
:   [callJS](func_TJSObject_callJS) (Javascript インスタンスメソッドの呼び出し )
    [callSQ](func_TJSObject_callSQ) (Squirrel インスタンスメソッドの呼び出し )

プロパティ