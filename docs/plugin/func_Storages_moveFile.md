# Storages.moveFile

機能/意味
:   指定ファイルを移動する。

タイプ
:   [Storagesクラス](class_Storages)のメソッド

構文
:   moveFile(fromFile, toFile)

引数
:   |  |  |
    | --- | --- |
    | fromFile | 移動対象ファイル |
    | toFile | 移動先パス |

戻り値
:   実際に移動されたら true\n移動対象ファイルが実在し、移動先パスにファイルが無い場合のみ移動されます

説明