# Array.unshift

機能/意味
:   要素を配列の先頭に追加します。

タイプ
:   [Arrayクラス](class_Array)のメソッド

構文
:   unshift(value, ...) :int

引数
:   |  |  |
    | --- | --- |
    | value | 追加値 |

戻り値
:   追加後のサイズ

説明
:   複数の要素が指定された場合は複数の要素を先頭に追加することができます。
    この場合は、たとえば a, b, c の順番で渡された場合は、配列の先頭の要素は a, b, c の順番になります。