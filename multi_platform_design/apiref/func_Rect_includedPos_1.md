# Rect.includedPos

機能/意味
:   矩形内包判定

タイプ
:   [Rectクラス](class_Rect)のメソッド

構文
:   includedPos(x, y)

引数
:   |  |  |
    | --- | --- |
    | x | 判定するX軸座標値を指定します。 |
    | y | 判定するY軸座標値を指定します。 |

戻り値
:   内包有無

説明
:   指定の位置を内包するか判定します。
    内包する場合 true が返ります。
    right, bottom は含まない範囲で判定されます。