# Array.pop

機能/意味
:   配列の最後から一つ要素を取り出し、それを返します。

タイプ
:   [Arrayクラス](class_Array)のメソッド

構文
:   pop() :variant

引数

戻り値
:   取り出された要素を返します。要素が無かった場合はvoidを返します。

説明
:   取り出された要素は配列から削除されます(配列のサイズは一つ減ります)。