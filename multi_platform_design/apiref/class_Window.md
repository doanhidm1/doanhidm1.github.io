# Window

Window クラスは、ウィンドウを管理するためのクラスです。
ウインドウのサイズ制御はWindowsでのみ可能です。
Androidでは画面サイズに依存します。
Offscreenクラスをレンダリングしたいサイズで生成し、そこに通常の描画を行います。
ウインドウ(画面)サイズに応じた拡大等はスクリプトで制御します。
UIや文字などを等倍で綺麗に表示することもスクリプトで可能となります。

AndroidがサポートしていないイベントはAndroidでは発生しません。

# メンバ

コンストラクタ
:   [Window](func_Window_Window) (Window オブジェクトの構築 )

メソッド
:   [add](func_Window_add) (管理オブジェクトの追加 )
    [bringToFront](func_Window_bringToFront) ([Windows\*]ウィンドウを最前面に移動 )
    [close](func_Window_close) (ウィンドウを閉じる )
    [getMouseVelocity](func_Window_getMouseVelocity) ([Windows\*]マウス座標移動速度の取得 )
    [getTouchPoint](func_Window_getTouchPoint) (タッチ座標の取得 )
    [getTouchVelocity](func_Window_getTouchVelocity) (タッチ座標移動速度の取得 )
    [hideMouseCursor](func_Window_hideMouseCursor) ([Windows\*]マウスカーソルを一時的に隠す )
    [postInputEvent](func_Window_postInputEvent) (入力イベントの生成 )
    [registerMessageReceiver](func_Window_registerMessageReceiver) ([Windows+]メッセージ受信関数の登録/登録削除 )
    [remove](func_Window_remove) (管理オブジェクトの削除 )
    [removeMaskRegion](func_Window_removeMaskRegion) ([Windows+]ウィンドウリージョンの解除 )
    [resetMouseVelocity](func_Window_resetMouseVelocity) ([Windows\*]マウス座標移動速度計測のリセット )
    [setInnerSize](func_Window_setInnerSize) ([Windows\*]クライアントサイズの設定 )
    [setMaskRegion](func_Window_setMaskRegion) ([Windows+]ウィンドウリージョンをマスクに従って設定 )
    [setMaxSize](func_Window_setMaxSize) ([Windows\*]ウィンドウの最大サイズの設定 )
    [setMinSize](func_Window_setMinSize) ([Windows\*]ウィンドウの最小サイズの設定 )
    [setPos](func_Window_setPos) ([Windows\*]ウィンドウ位置の設定 )
    [setSize](func_Window_setSize) ([Windows\*]ウィンドウサイズの設定 )
    [setZoom](func_Window_setZoom) ([Windows\*]レイヤ拡大倍率の設定 )
    [showModal](func_Window_showModal) ([Windows+]モーダルでウィンドウを表示 )
    [startDrawing](func_Window_startDrawing) (明示的に描画を呼び出す。 )
    [update](func_Window_update) ([Windows\*]ウィンドウ内容の強制的な描画 )

プロパティ
:   [borderStyle](prop_Window_borderStyle) ([Windows\*]ウィンドウ外見 )
    [canvas](prop_Window_canvas) (描画用の Canvas クラスのインスタンスを得る )
    [caption](prop_Window_caption) (ウィンドウのキャプション )
    [displayDensity](prop_Window_displayDensity) (画面密度(dpi) )
    [displayOrientation](prop_Window_displayOrientation) (ディスプレイの向き )
    [displayRotate](prop_Window_displayRotate) (ディスプレイの回転角度 )
    [drawCycle](prop_Window_drawCycle) (指定サイクルで描画を行う )
    [drawDevice](prop_Window_drawDevice) ([Windows+]描画デバイス )
    [enableTouch](prop_Window_enableTouch) (タッチイベント有効/無効 )
    [focusable](prop_Window_focusable) ([Windows\*]フォーカスを取得可能か )
    [focusedLayer](prop_Window_focusedLayer) ([Windows+]フォーカスを持っているレイヤオブジェクト )
    [fullScreen](prop_Window_fullScreen) ([Windows\*]フルスクリーンかどうか )
    [height](prop_Window_height) (ウィンドウの縦幅 )
    [hintDelay](prop_Window_hintDelay) ([Windows\*]ヒント表示待ち時間 )
    [HWND](prop_Window_HWND) ([Windows+]ウィンドウハンドル )
    [imeMode](prop_Window_imeMode) ([Windows\*]デフォルトのIMEモード )
    [innerHeight](prop_Window_innerHeight) (クライアント領域の縦幅 )
    [innerWidth](prop_Window_innerWidth) (クライアント領域の横幅 )
    [left](prop_Window_left) ([Windows\*]ウィンドウの左端位置 )
    [mainWindow](prop_Window_mainWindow) (メインウィンドウ )
    [maxHeight](prop_Window_maxHeight) ([Windows\*]ウィンドウの最大の縦幅 )
    [maxWidth](prop_Window_maxWidth) ([Windows\*]ウィンドウの最大の横幅 )
    [minHeight](prop_Window_minHeight) ([Windows\*]ウィンドウの最小の縦幅 )
    [minWidth](prop_Window_minWidth) ([Windows\*]ウィンドウの最小の横幅 )
    [mouseCursorState](prop_Window_mouseCursorState) ([Windows\*]マウスカーソル表示状態 )
    [primaryLayer](prop_Window_primaryLayer) ([Windows+]プライマリレイヤオブジェクト )
    [stayOnTop](prop_Window_stayOnTop) ([Windows\*]常に最上位に表示するかどうか )
    [top](prop_Window_top) ([Windows\*]ウィンドウの上端位置 )
    [touchPointCount](prop_Window_touchPointCount) (タッチ数 )
    [touchRotateThreshold](prop_Window_touchRotateThreshold) (マルチタッチ回転閾値 )
    [touchScaleThreshold](prop_Window_touchScaleThreshold) (マルチタッチ拡大閾値 )
    [trapKey](prop_Window_trapKey) ([Windows\*]キー入力をトラップするか )
    [useMouseKey](prop_Window_useMouseKey) ([Windows\*]マウスキーを使用するかどうか )
    [visible](prop_Window_visible) ([Windows\*]ウィンドウが表示されているかどうか )
    [waitVSync](prop_Window_waitVSync) (垂直同期待ち )
    [width](prop_Window_width) (ウィンドウの横幅 )
    [zoomDenom](prop_Window_zoomDenom) ([Windows\*]レイヤ拡大倍率(分母) )
    [zoomNumer](prop_Window_zoomNumer) ([Windows\*]レイヤ拡大倍率(分子) )

イベント
:   [onActivate](event_Window_onActivate) (ウィンドウがアクティブになったとき )
    [onClick](event_Window_onClick) ([Windows\*]ウィンドウがクリックされた )
    [onCloseQuery](event_Window_onCloseQuery) ([Windows\*]ウィンドウを閉じる確認 )
    [onDeactivate](event_Window_onDeactivate) (ウィンドウが非アクティブになったとき )
    [onDisplayRotate](event_Window_onDisplayRotate) (画面が回転されたとき )
    [onDoubleClick](event_Window_onDoubleClick) ([Windows\*]ウィンドウがダブルクリックされた )
    [onDraw](event_Window_onDraw) (画面更新コールバックイベント )
    [onFileDrop](event_Window_onFileDrop) ([Windows\*]ファイルがドロップされた )
    [onHintChanged](event_Window_onHintChanged) ([Windows\*]ヒントの状態が変化したとき )
    [onKeyDown](event_Window_onKeyDown) (キーが押された )
    [onKeyPress](event_Window_onKeyPress) ([Windows\*]文字が入力された )
    [onKeyUp](event_Window_onKeyUp) (キーが離された )
    [onMouseDown](event_Window_onMouseDown) ([Windows\*]マウスのボタンが押された )
    [onMouseEnter](event_Window_onMouseEnter) ([Windows\*]マウスが入ってきた )
    [onMouseLeave](event_Window_onMouseLeave) ([Windows\*]マウスが出ていった )
    [onMouseMove](event_Window_onMouseMove) ([Windows\*]マウスが移動した )
    [onMouseUp](event_Window_onMouseUp) ([Windows\*]マウスのボタンが離された )
    [onMouseWheel](event_Window_onMouseWheel) ([Windows\*]マウスホイールが回転した )
    [onMultiTouch](event_Window_onMultiTouch) (マルチタッチ状態が変化した )
    [onPointerDown](event_Window_onPointerDown) (何らかのポインティングデバイスで押下された )
    [onPointerMove](event_Window_onPointerMove) (何らかのポインティングデバイスが移動された )
    [onPointerUp](event_Window_onPointerUp) (何らかのポインティングデバイスが離された )
    [onPopupHide](event_Window_onPopupHide) ([Windows\*]ポップアップウィンドウを閉じる )
    [onResize](event_Window_onResize) ([Windows\*]ウィンドウのサイズが変化した )
    [onTouchDown](event_Window_onTouchDown) (画面がタッチされた )
    [onTouchMove](event_Window_onTouchMove) (指が移動した )
    [onTouchRotate](event_Window_onTouchRotate) (回転操作した )
    [onTouchScaling](event_Window_onTouchScaling) (拡大操作した )
    [onTouchUp](event_Window_onTouchUp) (画面から指が離された )