# VideoOverlay.loop

機能/意味
:   [Windows\*]ループ再生をするかどうか

タイプ
:   [VideoOverlayクラス](class_VideoOverlay)のプロパティ

説明
:   ループ再生の有効無効を表します。
    値を設定することもできます。
    真ならばループ、偽ならば非ループです。
    Androidでは常に偽を返し、設定しても意味はありません。