# String.［］

機能/意味
:   '数値' プロパティ

タイプ
:   [Stringクラス](class_String)のプロパティ

説明
:   数値をプロパティとして間接指定すると、その文字位置にある文字にアクセスできます。
    たとえば、str が文字列あるいは文字列を表す式だとすると、str[0] は先頭の一文字を表す文字列になります。
    数値が文字列よりも長い位置や、負の場合は例外が発生します ( ただし文字列の最後の文字は 空文字 が返されます )。

    代入式を生成することはできません。