# Octet.［］

機能/意味
:   '数値' プロパティ

タイプ
:   [Octetクラス](class_Octet)のプロパティ

説明
:   数値をプロパティとして間接指定すると、その位置にあるオクテットにアクセスできます。
    結果はオクテットの数値で表されます。
    たとえば、oct がオクテット列を表す式だとすると、oct[0] は先頭のオクテットの値を表す数値になります。
    数値がオクテット列よりも長い位置や、負の場合は例外が発生します。

    代入式を生成することはできません。